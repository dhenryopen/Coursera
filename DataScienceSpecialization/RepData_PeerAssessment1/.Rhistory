q()
```{r}
ggplot(total_steps_per_day, aes(x=steps)) + geom_histogram()
```
qplot(total_steps_per_day$date, total_steps_per_day$steps)
source('~/.active-rstudio-document', echo=TRUE)
setwd("C:/Users/Dave/Google Drive/Coursera/Reproducible Research/RepData_PeerAssessment1")
source('~/.active-rstudio-document', echo=TRUE)
source('~/.active-rstudio-document', echo=TRUE)
head(activity_df)
source('~/.active-rstudio-document', echo=TRUE)
total_steps_per_day <- activity_df[c(1,2)] %>% group_by(date)
total_steps_per_day %>% summarise(total_steps=sum(steps))
mean_steps_per_day <- activity_df[c(1,2)] %>% group_by(date)
mean_steps_per_day %>% summarise(mean_steps=mean(steps))
mean_steps_per_day <- activity_df[c(1,2)] %>% group_by(date) %>% summarise(mean_steps=mean(steps))
source('~/.active-rstudio-document', echo=TRUE)
source('~/.active-rstudio-document', echo=TRUE)
average_daily_activity_pattern <- activity_df[c(1,3)] %>% group_by(interval) >> summarise(mean_activity_interval=mean(steps))
average_daily_activity_pattern <- activity_df[c(1,3)] %>% group_by(interval) %>% summarise(mean_activity_interval=mean(steps))
View(average_daily_activity_per_interval)
source('~/.active-rstudio-document', echo=TRUE)
View(average_daily_activity_pattern)
View(average_daily_activity_pattern)
View(mean_steps_per_day)
View(mean_steps_per_day)
View(median_steps_per_day)
View(median_steps_per_day)
View(total_steps_per_day)
View(total_steps_per_day)
View(mean_steps_per_day)
ggplot(total_steps_per_day, aes(x=date) + geom_histogram())
ggplot(total_steps_per_day, aes(x=total_steps_per_day$date) + geom_histogram())
View(total_steps_per_day)
View(total_steps_per_day)
ggplot(total_steps_per_day, aes(y=total_steps) + geom_histogram())
ggplot(total_steps_per_day, aes(x=total_steps) + geom_histogram())
ggplot(total_steps_per_day, aes(x=total_steps)) + geom_histogram()
ggplot(total_steps_per_day, aes(y=total_steps)) + geom_histogram()
ggplot(total_steps_per_day, aes(x=date)) + geom_histogram()
ggplot(total_steps_per_day, aes(x=date, y=total_steps)) + geom_histogram()
hist(total_steps_per_day$date,total_steps_per_day$total_steps)
hist(total_steps_per_day$date,total_steps_per_day$total_steps,breaks=10)
total_steps_per_day
hist(total_steps_per_day$date,total_steps_per_day$total_steps,breaks=60)
hist(total_steps_per_day$total_steps,total_steps_per_day$date,breaks=60)
hist(activity_df$date,activity_df$steps)
hist(activity_df$date,activity_df$steps, breaks=60)
hist(total_steps_per_day$total_steps,total_steps_per_day$date,breaks=10)
hist(activity_df$date,activity_df$steps, breaks=10)
hist(activity_df$date,activity_df$steps, breaks=30)
hist(total_steps_per_day$date,total_steps_per_day$total_steps,breaks=30)
hist(activity_df$date,activity_df$steps, breaks=30)
qplot(total_steps_per_day$date, total_steps_per_day$total_steps)
hist(total_steps_per_day$date,total_steps_per_day$total_steps,breaks=60)
hist(activity_df$steps)
ggplot(total_steps_per_day, aes(date,total_steps)) + geom_histogram()
ggplot(total_steps_per_day, aes(date,total_steps)) + geom_histogram(stat="bin")
ggplot(total_steps_per_day, aes(date)) + geom_histogram(stat="bin")
ggplot(total_steps_per_day, aes(x=date)) + geom_histogram(stat="bin")
ggplot(total_steps_per_day, aes(x=date)) + geom_histogram(data=total_steps,stat="bin")
ggplot(total_steps_per_day, aes(x=date)) + geom_histogram(data=total_steps_per_day$total_steps,stat="bin")
hist(total_steps_per_day$date,total_steps_per_day$total_steps,breaks=60)
hist(activity_df$steps)
activity_df$steps
activity_df$date
total_steps_per_day$date
total_steps_per_day$total_steps
qplot(total_steps_per_day$date, total_steps_per_day$total_steps)
qplot(total_steps_per_day$date, total_steps_per_day$total_steps) + geom_histogram()
qplot(total_steps_per_day$date, total_steps_per_day$total_steps) + geom_bar()
hist(activity_df$date ~ activity_df$steps, breaks=30)
hist(activity_df$steps)
hist(activity_df$steps,
breaks=seq(from=0, to=25000, by=2500),
col="blue",
xlab="Total number of steps",
ylim=c(0, 20),
main="Histogram of the total number of steps taken each day\n(NA removed)")
hist(total_steps_per_day$total_steps,
breaks=seq(from=0, to=25000, by=2500),
col="blue",
xlab="Total number of steps",
ylim=c(0, 20),
main="Histogram of the total number of steps taken each day\n(NA removed)")
hist(total_steps_per_day$total_steps,
breaks=seq(from=0, to=25000, by=2500),
xlab="Total number of steps",
ylim=c(0, 20),
main="Total number of steps taken each day\n(excluding NA)")
hist(total_steps_per_day$total_steps,
breaks=seq(from=0, to=25000, by=2500),
xlab="Total number of steps",
ylim=c(0, 20),
main="Total # of Steps Taken Each Day\n(Excluding NA)")
source('~/.active-rstudio-document', echo=TRUE)
